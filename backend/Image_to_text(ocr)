<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image to Text App - Notes</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }

        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .header p {
            font-size: 1.1em;
            opacity: 0.95;
        }

        .content {
            padding: 40px;
        }

        .section {
            margin-bottom: 40px;
            padding: 25px;
            background: #f8f9fa;
            border-radius: 15px;
            border-left: 5px solid #667eea;
        }

        .section h2 {
            color: #667eea;
            font-size: 1.8em;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .section h3 {
            color: #764ba2;
            font-size: 1.3em;
            margin: 20px 0 10px 0;
        }

        .section p, .section li {
            color: #333;
            line-height: 1.8;
            font-size: 1.05em;
        }

        .emoji {
            font-size: 1.2em;
        }

        .code-block {
            background: #2d2d2d;
            color: #f8f8f2;
            padding: 20px;
            border-radius: 10px;
            margin: 15px 0;
            overflow-x: auto;
            font-family: 'Courier New', monospace;
            font-size: 0.95em;
            line-height: 1.6;
        }

        .highlight {
            background: #fff3cd;
            padding: 3px 8px;
            border-radius: 5px;
            color: #856404;
            font-weight: 600;
        }

        .flow-box {
            background: white;
            padding: 20px;
            border-radius: 10px;
            margin: 15px 0;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }

        .flow-step {
            display: flex;
            align-items: center;
            gap: 15px;
            margin: 10px 0;
            padding: 15px;
            background: linear-gradient(to right, #667eea15, #764ba215);
            border-radius: 8px;
        }

        .step-number {
            background: #667eea;
            color: white;
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            flex-shrink: 0;
        }

        .use-cases {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 15px;
        }

        .use-case {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            transition: transform 0.3s;
        }

        .use-case:hover {
            transform: translateY(-5px);
        }

        .use-case .emoji {
            font-size: 2.5em;
            margin-bottom: 10px;
        }

        ul {
            margin-left: 25px;
            margin-top: 10px;
        }

        ul li {
            margin: 8px 0;
        }

        .arrow {
            color: #667eea;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>📸 Image to Text App</h1>
            <p>Complete OCR Backend Documentation</p>
        </div>

        <div class="content">
            <div class="section">
                <h2><span class="emoji">1️⃣</span> What is this project?</h2>
                <p>This is an <strong>Image to Text (OCR) API</strong>.</p>
                <p style="margin-top: 15px;"><span class="highlight">OCR = Optical Character Recognition</span></p>
                <p style="margin-top: 10px;">It means:<br><span class="arrow">➡️</span> Read text written inside an image (photo, screenshot, scanned paper)</p>
                
                <div class="flow-box" style="margin-top: 20px;">
                    <p><strong>Example:</strong></p>
                    <div style="margin-top: 10px; padding: 15px; background: #f0f0f0; border-radius: 8px;">
                        Upload image of notes / document <span style="font-size: 1.5em;">→</span> API returns the text written in that image
                    </div>
                </div>
            </div>

            <div class="section">
                <h2><span class="emoji">2️⃣</span> Required Packages (What each one does)</h2>
                
                <div class="code-block">const express = require("express");</div>
                <p><span class="emoji">👉</span> Creates a server and API routes</p>

                <div class="code-block">const cors = require("cors");</div>
                <p><span class="emoji">👉</span> Allows frontend (React / browser) to call backend APIs<br>(Without this, browser blocks request)</p>

                <div class="code-block">const multer = require("multer");</div>
                <p><span class="emoji">👉</span> Handles file uploads (images)</p>

                <div class="code-block">const path = require("path");</div>
                <p><span class="emoji">👉</span> Helps work with file paths & extensions</p>

                <div class="code-block">const Tesseract = require("tesseract.js");</div>
                <p><span class="emoji">👉</span> OCR library → extracts text from images</p>

                <div class="code-block">const fs = require("fs");</div>
                <p><span class="emoji">👉</span> File system → create folders, delete files</p>
            </div>

            <div class="section">
                <h2><span class="emoji">3️⃣</span> Create Express App</h2>
                <div class="code-block">const app = express();<br>app.use(cors());</div>
                <ul>
                    <li>app = your backend server</li>
                    <li>cors() allows frontend requests</li>
                </ul>
            </div>

            <div class="section">
                <h2><span class="emoji">4️⃣</span> Upload Folder Creation</h2>
                <div class="code-block">const uploadPath = "uploads";<br>if (!fs.existsSync(uploadPath)) fs.mkdirSync(uploadPath);</div>
                <p><span class="emoji">👉</span> Checks:</p>
                <ul>
                    <li>If uploads folder does not exist</li>
                    <li>Then create uploads folder</li>
                </ul>
                <p style="margin-top: 10px;">This is where images are temporarily stored.</p>
            </div>

            <div class="section">
                <h2><span class="emoji">5️⃣</span> Multer Storage Configuration (IMPORTANT)</h2>
                <div class="code-block">const storage = multer.diskStorage({
  destination: (req, file, cb) => {
    cb(null, uploadPath);
  },
  filename: (req, file, cb) => {
    cb(null, Date.now() + path.extname(file.originalname));
  },
});</div>
                <p><strong>What this does:</strong></p>
                <ul>
                    <li><strong>destination</strong> → where file is saved → <span class="highlight">uploads/</span></li>
                    <li><strong>filename</strong> → unique name using timestamp</li>
                </ul>
                <p style="margin-top: 15px;"><strong>Example:</strong></p>
                <div class="code-block">1723456789123.png</div>
                <p><strong>Why unique name?</strong><br><span class="emoji">👉</span> Avoid file name conflict</p>
            </div>

            <div class="section">
                <h2><span class="emoji">6️⃣</span> Multer Middleware</h2>
                <div class="code-block">const upload = multer({ storage });</div>
                <p>This connects multer with your storage settings.</p>
            </div>

            <div class="section">
                <h2><span class="emoji">7️⃣</span> Main API Route (Heart of the Code)</h2>
                <div class="code-block">app.post("/api/upload", upload.array("images", 10), async (req, res) => {</div>
                <p><strong>Meaning:</strong></p>
                <ul>
                    <li>POST API → <span class="highlight">/api/upload</span></li>
                    <li><strong>upload.array("images", 10)</strong></li>
                    <li>Accepts multiple images</li>
                    <li>Field name must be <span class="highlight">images</span></li>
                    <li>Max 10 images</li>
                </ul>
                <p style="margin-top: 15px;"><strong>Frontend must send:</strong></p>
                <div class="code-block">&lt;input type="file" name="images" multiple /&gt;</div>
            </div>

            <div class="section">
                <h2><span class="emoji">8️⃣</span> Check if Images Exist</h2>
                <div class="code-block">if (!req.files || req.files.length === 0) {
  return res.status(400).json({ message: "No images uploaded." });
}</div>
                <p><span class="emoji">👉</span> If no images uploaded → return error</p>
            </div>

            <div class="section">
                <h2><span class="emoji">9️⃣</span> Loop Through Each Image</h2>
                <div class="code-block">for (const file of req.files) {</div>
                <p>This processes images one by one.</p>
            </div>

            <div class="section">
                <h2><span class="emoji">🔟</span> OCR Text Extraction</h2>
                <div class="code-block">const { data } = await Tesseract.recognize(file.path, "eng");</div>
                <p><strong>What happens here:</strong></p>
                <ul>
                    <li>Reads image from uploads/</li>
                    <li>Uses English language OCR</li>
                    <li>Extracts text</li>
                </ul>
                <p style="margin-top: 15px;"><strong>Output looks like:</strong></p>
                <div class="code-block">data.text</div>
            </div>

            <div class="section">
                <h2><span class="emoji">1️⃣1️⃣</span> Store Result</h2>
                <div class="code-block">results.push({
  image: file.originalname,
  text: data.text.trim() || "No text detected",
});</div>
                <p><strong>Final response format:</strong></p>
                <div class="code-block">{
  "image": "note.png",
  "text": "This is extracted text"
}</div>
            </div>

            <div class="section">
                <h2><span class="emoji">1️⃣2️⃣</span> Delete Image After Processing</h2>
                <div class="code-block">fs.unlinkSync(file.path);</div>
                <p><span class="emoji">👉</span> Deletes image after OCR<br>✔️ Saves server storage<br>✔️ Clean backend</p>
            </div>

            <div class="section">
                <h2><span class="emoji">1️⃣3️⃣</span> Error Handling Per Image</h2>
                <div class="code-block">catch (err) {
  results.push({
    image: file.originalname,
    text: "Error extracting text",
  });
}</div>
                <p>If OCR fails → it doesn't crash server<br>Still returns result for other images</p>
            </div>

            <div class="section">
                <h2><span class="emoji">1️⃣4️⃣</span> Final Response to Client</h2>
                <div class="code-block">res.json({ results });</div>
                <p><strong>Example response:</strong></p>
                <div class="code-block">{
  "results": [
    {
      "image": "img1.png",
      "text": "Hello World"
    },
    {
      "image": "img2.jpg",
      "text": "No text detected"
    }
  ]
}</div>
            </div>

            <div class="section">
                <h2><span class="emoji">1️⃣5️⃣</span> Start Server</h2>
                <div class="code-block">app.listen(5000, () => console.log("Server running on port 5000"));</div>
                <p><span class="emoji">👉</span> Server runs at:</p>
                <div class="code-block">http://localhost:5000</div>
            </div>

            <div class="section">
                <h2><span class="emoji">🔁</span> Complete Flow (Very Simple)</h2>
                <div class="flow-box">
                    <div class="flow-step">
                        <div class="step-number">1</div>
                        <div>Frontend uploads images</div>
                    </div>
                    <div class="flow-step">
                        <div class="step-number">2</div>
                        <div>Multer saves images in uploads/</div>
                    </div>
                    <div class="flow-step">
                        <div class="step-number">3</div>
                        <div>Tesseract reads image</div>
                    </div>
                    <div class="flow-step">
                        <div class="step-number">4</div>
                        <div>Text extracted</div>
                    </div>
                    <div class="flow-step">
                        <div class="step-number">5</div>
                        <div>Image deleted</div>
                    </div>
                    <div class="flow-step">
                        <div class="step-number">6</div>
                        <div>Text sent back to frontend</div>
                    </div>
                </div>
            </div>

            <div class="section">
                <h2><span class="emoji">✅</span> Real Use Cases</h2>
                <div class="use-cases">
                    <div class="use-case">
                        <div class="emoji">📝</div>
                        <p><strong>Scan handwritten notes</strong></p>
                    </div>
                    <div class="use-case">
                        <div class="emoji">📱</div>
                        <p><strong>Read text from screenshots</strong></p>
                    </div>
                    <div class="use-case">
                        <div class="emoji">📄</div>
                        <p><strong>Extract text from documents</strong></p>
                    </div>
                    <div class="use-case">
                        <div class="emoji">📷</div>
                        <p><strong>OCR feature in apps (like CamScanner)</strong></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>